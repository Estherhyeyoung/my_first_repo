예를 들어 출력하고자 하는 것이 
print array : 1 2 3 4 5 6 7 8
find number 2 where is ? 
found at the 1 in array.  일때 
 
coding에서는 
printf(" print array : "); 를 for문을 사용하기 전에 한번만 주어야 함. for문안에 넣어주었을 경우 for 조건문을 실행하는 동안 계속 출력이 됨. 

 binsearch 함수의 역할은 첫번째 인자x를 nrodml v라는 배열의 몇번째 있는 것이 인자x인지 찾아주는 함수이다. 
 binsearch함수는 반환타입이  int형이고 인자를 3개 갖는다. 
 첫번째 인자는 int형 인자이고, 두번째는 int형 배열이고 세번째로 int형 인자를 갖는다. 

 함수는 int main에 선언해주고 main밖에 함수를 써준다.  main 안에서 함수를 부를때는 같은 형식으로 불러주어야 한다. 
do while문 
do 아래의 조건문을 먼저 실행 후 while문을 실행 
break - 조건문을 실행했으면break에서 끝내는것 (for, while, do, switch)
continue :  반복문(loop)에서 실행되며, 주로 무언가 제외하고 실행하고 싶을때 사용되며, switch문에서는 사용할 수 없다.
goto함수 - 
goto함수가 지정한 다른 곳으로 옮겨가는 것. -건너뛰는 함수/ 에러일때 사용할수 있음. 에러가난것을 찾기 힘듦

HW shellsort 그림으로 그려서 표현하기 
