bitcount함수는 bit는 0과 1로 이루어진 2진수 숫자이며, 
	예를 들어 아라비아 숫자10을 넣었을때 아라비아 숫자 10을 컴퓨터의 2진수로 이루어진 숫자에서 1이 몇개인지 카운트하기 위한 함수이다.

expr1 ? expr2 : expr3 
expr1 의 조건이 참이면 expr2 실행시키고 expr1의조건이 거짓이면 expr3을 실행시켜라 

 if ()-else - if의 조건이 참이면 아래의 조건을 실행시키고 if의 조건이 참이 아니면 else의 조건을 실행시켜라 
 else-if() if 조건이 참이 아니면 else if의 조건이 아니면 else() 를 실행 시켜라 . 
 else-if는 if의 하위 조건임. 
