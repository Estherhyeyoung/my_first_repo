position key H~l 
close :q!
save and close :wq
같은 명령어나 단어 입력할때 첫 몇글자ex : ali+tab 누르면 자동완성 
command+ ~ : 창 이동 
새로운 창 열기 : N 
현재 있는 위치 확인 : ls
맨 아래페이지 gg
맨 위페이지 G
한 페이지에서 중간 : m, 마지막 : l, 위 : h
: set number 줄 넘버링  
esc : 초기화
단어단위로 넘기기 w, 단어의 끝으로 넘기기 e , 뒤로가기 : b 
지우기 x , 실행 취소 u , control+r 
수정할때 : r - replace 
/찾는 단어, 커서아래부터 찾기 
? 문서의 아래에서 부터 위로 찾기 
:set hlsearch  찾는 단어 색으로 표시 / :set nohlsearch 이전 상태
N: 이전에 내가 찾았던 단어 n: next 찾는 단어로 이동   
% : 함수에서 짝을 찾아줌  
v : visual ; drag 역할 , 단어를 선택하겠다 하면 vw line선택 : shift-v 
copy : y , paste : p 
dd : delete, 

#include <stdio.h>
// print fahrenheit- Celsius table- 화씨와 도씨를선언하기 
int 데이터의 단위, 정수, float 실제 수 
엔트리 전에 메크로 고정값을 주는것은 #define UPPER(꼭 대문자로 작성해야함)
while 문 : while문 안에 있는 함수를 종료시점까지 반복하는 것. 
가장 중요한것 시작과, 언제 끝나는지 꼭 확인!
while 대신 for문으로 사용할 수 있음 
예 : for (fahr = LOWER; fahr ,= UPPER; fahr + STEP)
%d : 10진수의 숫자 
%6d : 정수 6자리 까지 보여주기 
%f : 소수점 표시 
%.2f : 소수점 2자리까지 보여주기
C= getchar(); 사용자가 입력하는 글자를 가져오는 것
putchar(c); 사용자가 입력한 글자를 보여주는 것
++ 1을 더해주는 것, != 같지 않다 == 같다 

특히, 함수를 넣을때 큰괄호와 작은괄호의 짝이 어디있는지 확인을 하고 큰 괄호는 각각의 줄에 삽입하는 것이 좋다. 

